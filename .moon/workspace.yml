$schema: ./cache/schemas/workspace.json

projects:
  - "apps/*/moon.yml"

defaultProject: website

vcs:
  defaultBranch: main
  provider: github

remote:
  api: "http"
  host: "https://remoshu.carere.workers.dev"
  auth:
    token: "MOON_REMOTE_CACHE_TOKEN"
  cache:
    instanceName: "carere/zaidan"
    verifyIntegrity: true
