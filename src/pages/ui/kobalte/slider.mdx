---
title: Slider
slug: slider
description: An input where the user selects a value from within a given range.
component: true
links:
  doc: https://base-ui.com/react/components/slider
  api: https://base-ui.com/react/components/slider#api-reference
---

<ComponentPreview name="slider-demo" />

## Installation

<CodeTabs>

<TabsList variant="line" class="gap-6">
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-dlx
shadcn@latest add @zaidan/slider
```

</TabsContent>

<TabsContent value="manual">

<Steps class="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```package-install
solid-js
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="slider" title="components/ui/slider.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { Slider } from "~/components/ui/slider";
```

```tsx showLineNumbers
<Slider defaultValue={[33]} max={100} step={1} />
```

## Range

Use an array with two values for a range slider.

<ComponentPreview name="slider-range" />

## Multiple Thumbs

Use an array with multiple values for multiple thumbs.

<ComponentPreview name="slider-multiple" />

## Vertical

Use `orientation="vertical"` for a vertical slider.

<ComponentPreview name="slider-vertical" />

## Controlled

<ComponentPreview name="slider-controlled" />

## Disabled

Use the `disabled` prop to disable the slider.

<ComponentPreview name="slider-disabled" />

## API Reference

See the [Base UI Slider API reference](https://base-ui.com/react/components/slider#api-reference) for the pinned public API, keyboard behavior, and accessibility contract.
