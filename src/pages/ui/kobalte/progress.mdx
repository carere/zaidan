---
title: Progress
slug: progress
description: Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.
component: true
foundation: kobalte
links:
  doc: https://kobalte.dev/docs/core/components/progress
  api: https://kobalte.dev/docs/core/components/progress#api-reference
---

The Progress component is built on top of [Kobalte Progress](https://kobalte.dev/docs/core/components/progress), which provides the accessible progress primitive.

<ComponentPreview name="progress-demo" />

## Installation

<CodeTabs>

<TabsList variant="line" class="gap-6">
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-dlx
shadcn@latest add @zaidan/progress
```

</TabsContent>

<TabsContent value="manual">

<Steps class="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```package-install
@kobalte/core
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="progress" title="components/ui/progress.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { Progress } from "~/components/ui/progress";
```

```tsx showLineNumbers
<Progress value={33} />
```

## Composition

### With label and value

Use `ProgressLabel` and `ProgressValue` to add a label and value display.

```tsx showLineNumbers
import {
  Progress,
  ProgressLabel,
  ProgressValue,
} from "~/components/ui/progress";

<Progress value={56} class="w-full max-w-sm">
  <ProgressLabel>Upload progress</ProgressLabel>
  <ProgressValue />
</Progress>
```

```text
Progress
├── ProgressLabel
├── ProgressValue
└── ProgressTrack
    └── ProgressIndicator
```

## Label

Use `ProgressLabel` and `ProgressValue` to add a label and value display.

<ComponentPreview name="progress-label" />

## Controlled

A progress bar that can be controlled by a slider.

<ComponentPreview name="progress-controlled" />

## Indeterminate

Use the `indeterminate` prop to show an indeterminate progress state.

<ComponentPreview name="progress-indeterminate" />

## Custom Value Range

Use `minValue` and `maxValue` to set custom value ranges.

<ComponentPreview name="progress-range" />

## API Reference

See the [Kobalte Progress API reference](https://kobalte.dev/docs/core/components/progress#api-reference) for primitive props, accessibility, and progress semantics.
