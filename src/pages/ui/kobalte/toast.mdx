---
title: Toast
slug: toast
description: An opinionated toast component.
component: true
foundation: kobalte
links:
  doc: https://github.com/wobsoriano/solid-sonner
---

The Toast component wraps [solid-sonner](https://github.com/wobsoriano/solid-sonner), a SolidJS port of [Sonner](https://sonner.emilkowal.ski/), and themes it to match your app.

<ComponentPreview name="toast-demo" />

## Installation

<CodeTabs>

<TabsList variant="line" class="gap-6">
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-dlx
shadcn@latest add @zaidan/toast
```

</TabsContent>

<TabsContent value="manual">

<Steps class="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```package-install
solid-sonner
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toast" title="components/ui/toast.tsx" />

<Step>Update the import paths to match your project setup.</Step>

<Step>Add the Toaster component to your application root.</Step>

```tsx title="app.tsx" showLineNumbers
import { Toaster } from "~/components/ui/toast";

export default function App() {
  return (
    <>
      <Toaster />
    </>
  );
}
```

</Steps>

</TabsContent>

</CodeTabs>

## Usage

Import `toast` from `solid-sonner` directly — the registry's `toast.tsx` only exports the themed `Toaster`, it does not re-export `toast` itself.

```tsx showLineNumbers
import { toast } from "solid-sonner";
```

```tsx showLineNumbers
toast("Event created", {
  description: "Sunday, December 3 at 9:00 AM",
});
```

## Types

Call `toast.success`, `toast.info`, `toast.warning`, or `toast.error` to render the matching status icon. `toast.loading` renders a spinner.

<ComponentPreview name="toast-types" />

## Action

Pass an `action` with a `label` and `onClick` to render a button on the toast.

```tsx showLineNumbers
toast("Event created", {
  action: {
    label: "Undo",
    onClick: () => {},
  },
});
```

## Promise

Use `toast.promise` to update one toast as an asynchronous task moves through loading, success, and error states.

<ComponentPreview name="toast-promise" />

## API Reference

### Toaster

The only export from `toast.tsx`. It forwards every prop to `solid-sonner`'s `Toaster` and additionally wires up the app's color mode, status icons, and CSS variables so toasts match your theme.

See the [solid-sonner README](https://github.com/wobsoriano/solid-sonner) for `Toaster` props (`position`, `richColors`, `duration`, `gap`, `visibleToasts`, `closeButton`, `dir`, and more) and the full `toast()` API (`toast()`, `toast.success()`, `toast.info()`, `toast.warning()`, `toast.error()`, `toast.loading()`, `toast.custom()`, `toast.message()`, `toast.promise()`, `toast.dismiss()`).
