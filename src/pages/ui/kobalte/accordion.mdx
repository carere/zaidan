---
title: Accordion
slug: accordion
description: A vertically stacked set of interactive headings that each reveal a section of content.
component: true
foundation: kobalte
links:
  doc: https://kobalte.dev/docs/core/components/accordion
  api: https://kobalte.dev/docs/core/components/accordion#api-reference
---

<ComponentPreview
  name="accordion-demo"
  align="start"
  previewClass="*:data-[slot=accordion]:max-w-sm min-h-[300px]"
/>

## Installation

<CodeTabs>

<TabsList variant="line" class="gap-6">
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-dlx
shadcn@latest add @zaidan/accordion
```

</TabsContent>

<TabsContent value="manual">

<Steps class="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```package-install
@kobalte/core lucide-solid
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="accordion" title="components/ui/accordion.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "~/components/ui/accordion";
```

```tsx showLineNumbers
<Accordion defaultValue={["item-1"]}>
  <AccordionItem value="item-1">
    <AccordionTrigger>Is it accessible?</AccordionTrigger>
    <AccordionContent>
      Yes. It follows the WAI-ARIA accordion pattern.
    </AccordionContent>
  </AccordionItem>
</Accordion>
```

## Composition

Use the following composition to build an `Accordion`:

```text
Accordion
├── AccordionItem
│   ├── AccordionTrigger
│   └── AccordionContent
└── AccordionItem
    ├── AccordionTrigger
    └── AccordionContent
```

## Basic

A basic accordion that shows one item at a time. The first item is open by default.

<ComponentPreview
  name="accordion-basic"
  align="start"
  previewClass="*:data-[slot=accordion]:max-w-sm min-h-[300px]"
/>

## Multiple

Use the `multiple` prop to allow multiple items to be open at the same time.

<ComponentPreview
  name="accordion-multiple"
  align="start"
  previewClass="*:data-[slot=accordion]:max-w-sm min-h-[360px]"
/>

## Disabled

Use the `disabled` prop on `AccordionItem` to disable individual items.

<ComponentPreview
  name="accordion-disabled"
  align="start"
  previewClass="*:data-[slot=accordion]:max-w-sm min-h-[300px]"
/>

## Borders

Add border utilities to the `Accordion` and `AccordionItem` components to visually group items.

<ComponentPreview
  name="accordion-borders"
  align="start"
  previewClass="*:data-[slot=accordion]:max-w-sm min-h-[300px]"
/>

## Card

Wrap the `Accordion` in a `Card` component.

<ComponentPreview
  name="accordion-card"
  align="start"
  previewClass="*:data-[slot=accordion]:max-w-sm min-h-[420px]"
/>

## API Reference

See the [Kobalte Accordion documentation](https://kobalte.dev/docs/core/components/accordion#api-reference) for the primitive API.
