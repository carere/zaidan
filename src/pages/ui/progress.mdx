---
title: Progress
slug: progress
description: Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.
---

# Progress

Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.

## Installation

### CLI

```package-dlx
shadcn@latest add progress
```

### Manual

Copy and paste the following code into your project.

```tsx file=../../registry/ui/progress.tsx showLineNumbers

```

## With Label

```tsx
import {
  Progress,
  ProgressLabel,
  ProgressValue,
} from "~/components/ui/progress";
```

```tsx showLineNumbers
<Progress value={75}>
  <div class="flex w-full justify-between">
    <ProgressLabel>Loading...</ProgressLabel>
    <ProgressValue />
  </div>
</Progress>
```

## Indeterminate

Use the `indeterminate` prop to show an indeterminate progress state.

```tsx showLineNumbers
<Progress indeterminate>
  <ProgressLabel>Processing...</ProgressLabel>
</Progress>
```

## Custom Value Range

Use `minValue` and `maxValue` to set custom value ranges.

```tsx showLineNumbers
<Progress value={5} minValue={0} maxValue={10}>
  <div class="flex w-full justify-between">
    <ProgressLabel>5 of 10 tasks completed</ProgressLabel>
    <ProgressValue />
  </div>
</Progress>
```

## Examples

Here are the source code of all the examples from the preview page:

### Progress Bar

```tsx
import { Progress } from "~/components/ui/progress";
```

```tsx file=../../registry/examples/progress-example.tsx#L26-L38

```

### With Label

```tsx
import { Progress, ProgressLabel, ProgressValue } from "~/components/ui/progress";
```

```tsx file=../../registry/examples/progress-example.tsx#L40-L49

```

### Controlled

```tsx
import { createSignal } from "solid-js";
import { Progress } from "~/components/ui/progress";
import { Slider } from "~/components/ui/slider";
```

```tsx file=../../registry/examples/progress-example.tsx#L51-L68

```

### File Upload List

```tsx
import { FileIcon } from "lucide-solid";
import { For } from "solid-js";
import {
  Item,
  ItemActions,
  ItemContent,
  ItemGroup,
  ItemMedia,
  ItemTitle,
} from "~/components/ui/item";
import { Progress } from "~/components/ui/progress";
```

```tsx file=../../registry/examples/progress-example.tsx#L70-L123

```
