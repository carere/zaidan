---
title: Resizable
slug: resizable
description: Accessible resizable panel groups and layouts with keyboard support.
---

# Resizable

Accessible resizable panel groups and layouts with keyboard support.

## Installation

### CLI

```package-dlx
shadcn@latest add @zaidan/resizable
```

### Manual

Install the following dependencies:

```package-install
@corvu/resizable
```

Copy and paste the following code into your project.

```tsx file=../../../registry/kobalte/ui/resizable.tsx showLineNumbers

```

## Examples

Here are the source code of all the examples from the preview page:

### Horizontal

```tsx
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "~/components/ui/resizable";
```

```tsx file=../../../registry/kobalte/examples/shadcn/resizable-example.tsx#L21-L39

```

### Vertical

Use the `orientation` prop to set the orientation of the resizable panels.

```tsx
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "~/components/ui/resizable";
```

```tsx file=../../../registry/kobalte/examples/shadcn/resizable-example.tsx#L41-L59

```

### With Handle

You can set or hide the handle by using the `withHandle` prop on the `ResizableHandle` component.

```tsx
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "~/components/ui/resizable";
```

```tsx file=../../../registry/kobalte/examples/shadcn/resizable-example.tsx#L61-L79

```

### Nested

You can nest `ResizablePanelGroup` components to create more complex layouts.

```tsx
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "~/components/ui/resizable";
```

```tsx file=../../../registry/kobalte/examples/shadcn/resizable-example.tsx#L81-L109

```

### Controlled

Use the `sizes` and `onSizesChange` props to control the panel sizes.

```tsx
import { createSignal } from "solid-js";
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "~/components/ui/resizable";
```

```tsx file=../../../registry/kobalte/examples/shadcn/resizable-example.tsx#L111-L138

```
