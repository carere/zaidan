---
title: Switch
slug: switch
description: A control that allows the user to toggle between checked and not checked.
---

# Switch

A control that allows the user to toggle between checked and not checked.

## Installation

### CLI

```package-dlx
shadcn@latest add @zaidan/switch
```

### Manual

Install the following dependencies:

```package-install
@kobalte/core
```

Copy and paste the following code into your project.

```tsx file=../../../registry/kobalte/ui/switch.tsx showLineNumbers

```

## Examples

Here are the source code of all the examples from the preview page:

### Basic

```tsx
import { Field, FieldLabel } from "~/components/ui/field";
import { Switch } from "~/components/ui/switch";
```

```tsx file=../../../registry/kobalte/examples/shadcn/switch-example.tsx#L23-L32

```

### With Description

```tsx
import {
  Field,
  FieldContent,
  FieldDescription,
  FieldLabel,
  FieldTitle,
} from "~/components/ui/field";
import { Switch } from "~/components/ui/switch";
```

```tsx file=../../../registry/kobalte/examples/shadcn/switch-example.tsx#L34-L50

```

### Disabled

```tsx
import { Label } from "~/components/ui/label";
import { Switch } from "~/components/ui/switch";
```

```tsx file=../../../registry/kobalte/examples/shadcn/switch-example.tsx#L52-L67

```

### Sizes

```tsx
import { Label } from "~/components/ui/label";
import { Switch } from "~/components/ui/switch";
```

```tsx file=../../../registry/kobalte/examples/shadcn/switch-example.tsx#L69-L84

```
