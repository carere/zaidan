---
title: Avatar
slug: avatar
description: An image element with a fallback for representing the user.
---

# Avatar

An image element with a fallback for representing the user.

## Installation

### CLI

```package-dlx
shadcn@latest add @zaidan/avatar
```

### Manual

Copy and paste the following code into your project.

```tsx file=../../../registry/kobalte/ui/avatar.tsx showLineNumbers

```

## Examples

Here are the source code of all the examples from the preview page:

### Sizes

```tsx
import {
  Avatar,
  AvatarFallback,
  AvatarImage,
} from "~/components/ui/avatar";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L36-L66

```

### Badge

```tsx
import {
  Avatar,
  AvatarBadge,
  AvatarFallback,
  AvatarImage,
} from "~/components/ui/avatar";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L68-L104

```

### Badge with Icon

```tsx
import { Check, Plus } from "lucide-solid";
import {
  Avatar,
  AvatarBadge,
  AvatarFallback,
  AvatarImage,
} from "~/components/ui/avatar";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L106-L154

```

### Group

```tsx
import {
  Avatar,
  AvatarFallback,
  AvatarGroup,
  AvatarImage,
} from "~/components/ui/avatar";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L156-L203

```

### Group with Count

```tsx
import {
  Avatar,
  AvatarFallback,
  AvatarGroup,
  AvatarGroupCount,
  AvatarImage,
} from "~/components/ui/avatar";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L205-L255

```

### Group with Icon Count

```tsx
import { Plus } from "lucide-solid";
import {
  Avatar,
  AvatarFallback,
  AvatarGroup,
  AvatarGroupCount,
  AvatarImage,
} from "~/components/ui/avatar";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L257-L317

```

### In Empty

```tsx
import { Plus } from "lucide-solid";
import {
  Avatar,
  AvatarFallback,
  AvatarGroup,
  AvatarGroupCount,
  AvatarImage,
} from "~/components/ui/avatar";
import { Button } from "~/components/ui/button";
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "~/components/ui/empty";
```

```tsx file=../../../registry/kobalte/examples/shadcn/avatar-example.tsx#L319-L363

```
